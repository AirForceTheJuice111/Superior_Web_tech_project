server:
  port: 8080

spring:
  application:
    name: mlplatform-backend

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
  python:
    base-url: http://localhost:8000
    timeout-ms: 5000
