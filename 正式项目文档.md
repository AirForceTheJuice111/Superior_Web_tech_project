# 交互式机器学习可视化学习平台正式项目文档

> 项目类型：传统机器学习算法仿真实验平台  
> 技术路线：Angular + Spring Boot + MyBatis + H2 + Python FastAPI + ECharts  
> 文档日期：2026-06-24

## 一、项目背景与目标

机器学习算法通常涉及特征空间、参数更新、损失函数、模型评估等抽象概念。对于初学者而言，仅通过公式或静态代码理解这些概念存在较高门槛。本项目面向课程实验与算法教学场景，构建一个交互式机器学习可视化学习平台，使学习者能够通过可视化实验观察模型训练过程。

本项目重点支持传统机器学习算法，包括监督学习、无监督学习和强化学习。平台通过算法选择、参数配置、单步训练、连续训练、可视化图表、指标面板、模型解释和实验记录等功能，帮助学习者理解模型如何从数据中学习规律。

项目目标如下：

1. 提供统一的机器学习实验配置界面。
2. 支持多种传统机器学习算法的逐步训练与可视化。
3. 支持 Loss、Accuracy、Silhouette 等指标动态展示。
4. 支持决策边界、回归线、聚类中心、树结构、GridWorld 等可视化。
5. 支持实验保存、历史记录、模型对比和预设实验案例。
6. 支持 AI 助教，为用户提供学习解释和调参建议。
7. 满足课程对 Angular、Spring Boot、MyBatis、数据库设计与云部署的要求。

## 二、需求分析

### 2.1 用户角色

| 用户角色 | 使用目标 |
|---|---|
| 学生 / 学习者 | 通过实验理解机器学习算法、调参、观察指标变化 |
| 教师 / 助教 | 用于课程演示、算法讲解和项目验收 |
| 课程项目评审者 | 检查系统功能、工程结构、部署效果和创新点 |

### 2.2 核心功能需求

1. 用户能够登录系统并进入学习仪表盘。
2. 用户能够按照课程路径选择机器学习知识点。
3. 用户能够在算法实验室选择学习类型、算法和数据集。
4. 用户能够调整算法超参数。
5. 用户能够上传小型 CSV 数据集。
6. 用户能够初始化训练、单步训练、自动训练、暂停训练、刷新状态。
7. 用户能够观察二维可视化图、Loss 曲线、Accuracy 曲线等。
8. 用户能够查看模型解释，如线性权重、决策树结构、聚类中心、主成分解释等。
9. 用户能够保存实验并查看历史记录。
10. 用户能够运行模型对比实验。
11. 用户能够通过 AI 助教获得学习解释。

### 2.3 非功能需求

1. 易用性：页面结构清晰，操作流程适合初学者。
2. 稳定性：部分服务不可用时，系统应尽量保持页面可访问并给出提示。
3. 可维护性：前端、后端、算法服务分层清楚，方便扩展算法和功能。
4. 可部署性：支持 Docker Compose 和云服务器部署。
5. 可演示性：系统需支持现场演示核心功能，并能通过文档说明运行方式。

## 三、总体架构设计

项目采用前后端分离加独立算法服务的三层架构：

```text
Angular Frontend
  ↓ REST API / SSE
Spring Boot Backend
  ↓ MyBatis / JDBC
H2 Database
  ↓ HTTP
Python FastAPI Training Service
```

### 3.1 前端层

前端使用 Angular 19 与 TypeScript 开发，主要负责：

- 页面展示和交互逻辑。
- 登录、仪表盘、课程路径、算法实验室、实验记录等页面组织。
- 调用后端 REST API。
- 使用 ECharts 展示二维可视化和指标曲线。
- 管理训练控制按钮、参数配置表单、CSV 上传等交互。
- 提供 AI 助教聊天入口。

### 3.2 后端层

后端使用 Spring Boot 3.3.5，主要负责：

- 提供 RESTful API。
- 管理用户登录。
- 管理算法元数据、数据集元数据、实验案例。
- 创建和管理训练 Session。
- 持久化实验记录。
- 通过 HTTP 调用 Python 算法服务。
- 提供 SSE 流式接口。
- 提供 AI 助教流式代理接口。

### 3.3 数据库层

数据库使用 H2 文件数据库，MyBatis 作为持久化层。数据库用于保存：

- 用户信息。
- 算法元数据。
- 数据集元数据。
- 训练会话状态。
- 实验记录。
- 预设实验案例。
- 用户上传的数据集信息。

### 3.4 算法服务层

算法服务使用 Python FastAPI 和 scikit-learn，主要负责：

- 维护训练会话。
- 执行单步训练或连续训练。
- 返回当前模型状态、指标、预测结果和可视化数据。
- 支持传统机器学习算法和强化学习示例。

## 四、系统模块设计

### 4.1 首页与认证模块

功能：

- 展示平台介绍。
- 引导用户进入学习流程。
- 支持登录。
- 支持注册，新用户的用户名、邮箱、密码写入数据库，密码使用 BCrypt 哈希存储。

主要文件：

- `src/app/features/workbench/workbench-page.component.ts`
- `src/app/features/auth/login-panel.component.ts`
- `src/app/core/services/auth-api.service.ts`
- `backend/src/main/java/com/example/mlplatform/controller/AuthController.java`
- `backend/src/main/java/com/example/mlplatform/dto/request/RegisterRequest.java`
- `backend/src/main/java/com/example/mlplatform/service/impl/AuthServiceImpl.java`

### 4.2 学习仪表盘模块

功能：

- 展示学习进度。
- 展示最近实验记录。
- 展示推荐学习任务。
- 展示学习日历、徽章和薄弱知识点。

主要文件：

- `src/app/features/workbench/workbench-page.component.ts`

### 4.3 课程路径 / 知识地图模块

功能：

- 按机器学习基础、监督学习、无监督学习、强化学习组织知识节点。
- 点击节点后展示简介、前置知识、推荐实验和练习。

主要文件：

- `src/app/features/workbench/workbench-page.component.ts`

### 4.4 算法实验室模块

功能：

- 选择学习类型、算法、数据集。
- 配置算法超参数。
- 上传 CSV 并选择特征列。
- 初始化训练。
- 单步执行、自动训练、暂停、刷新状态。
- 展示二维可视化、Loss 曲线、Accuracy 曲线、训练状态 JSON。
- 展示模型解释和评估指标。

主要文件：

- `src/app/features/config/experiment-config-panel.component.ts`
- `src/app/features/training/training-control-panel.component.ts`
- `src/app/features/training/evaluation-metrics-panel.component.ts`
- `src/app/features/training/model-explanation-panel.component.ts`
- `src/app/shared/components/two-dimensional-visualizer.component.ts`
- `src/app/shared/components/metric-trend-chart.component.ts`
- `src/app/shared/components/gridworld-visualizer.component.ts`

### 4.5 实验记录与模型对比模块

功能：

- 保存实验记录。
- 查询历史实验。
- 回填历史实验配置。
- 同数据集下对比不同算法或不同参数结果。

主要文件：

- `src/app/features/history/experiment-history-panel.component.ts`
- `src/app/features/comparison/model-comparison-panel.component.ts`
- `backend/src/main/java/com/example/mlplatform/controller/ExperimentController.java`

### 4.6 实验案例库模块

功能：

- 提供预设实验案例。
- 支持一键加载案例配置。
- 引导用户观察预期结果。

当前案例包括：

1. 线性回归：观察权重如何拟合数据。
2. 线性回归：学习率过大对收敛的影响。
3. SVM：鸢尾花二分类决策边界。
4. KMeans：K=3 的标准聚类过程。
5. KMeans：不同 K 值对聚类结果的影响。
6. Q-Learning：GridWorld 最优路径学习。

主要文件：

- `src/app/features/cases/experiment-case-library-panel.component.ts`
- `backend/src/main/java/com/example/mlplatform/controller/ExperimentCaseController.java`
- `backend/src/main/resources/data.sql`

### 4.7 AI 助教模块

功能：

- 提供右下角 AI 助教入口。
- 支持用户输入问题。
- 使用 SSE 流式返回回答。
- 可结合当前实验上下文提供学习解释。

主要文件：

- `src/app/shared/components/ai-assistant.component.ts`
- `src/app/core/services/ai-chat.service.ts`
- `src/app/core/services/experiment-context.service.ts`
- `backend/src/main/java/com/example/mlplatform/controller/AiController.java`
- `backend/src/main/java/com/example/mlplatform/service/AiService.java`

### 4.8 练习题模块

功能：

- 按课程大类、专题、试题三级展示练习。
- 后端提供题库，前端点击专题后返回题目（题目不含答案）。
- 学生提交后由服务端判分，记录每个专题最佳成绩并显示在专题入口。
- 课程路径页“查看相关练习”可直接进入对应专题的题目。

主要文件：

- `src/app/features/workbench/workbench-page.component.ts`
- `src/app/core/services/quiz-api.service.ts`
- `backend/src/main/java/com/example/mlplatform/controller/QuizController.java`
- `backend/src/main/java/com/example/mlplatform/service/impl/QuizServiceImpl.java`
- `backend/src/main/resources/data.sql`

## 五、算法功能实现

### 5.1 已支持算法

| 算法 | 学习类型 | 主要可视化 / 指标 |
|---|---|---|
| 线性回归 | 监督学习 | 回归线、Loss、MSE、RMSE、权重、Bias |
| SVM | 监督学习 | 决策边界、Hinge Loss、Accuracy、权重 |
| 逻辑回归 | 监督学习 | 概率边界、Log Loss、Accuracy、Precision、Recall、F1 |
| 决策树 | 监督学习 | 树结构、分裂节点、特征重要性、Accuracy |
| 随机森林 | 监督学习 | 树数量、特征重要性、Accuracy、Error Rate |
| KMeans | 无监督学习 | 聚类中心、簇分布、Inertia、Silhouette |
| PCA | 无监督学习 | 二维投影、解释方差、重构误差、主成分解释 |
| Q-Learning | 强化学习 | GridWorld、价值热力图、策略箭头、路径、成功率 |

### 5.2 算法服务实现方式

Python 算法服务为每个训练任务创建独立 Session。每次初始化训练后，后端会获得一个 sessionId。前端点击单步训练或自动训练时，后端调用 Python 服务更新模型状态，再把结果返回给前端。

训练状态包含：

- sessionId
- algorithm
- status
- currentStep
- maxSteps
- loss
- metrics
- parameters
- predictions
- visualization

前端根据这些字段更新图表、指标卡片和模型解释面板。

## 六、数据库设计

### 6.1 核心表

| 表名 | 用途 |
|---|---|
| `app_user` | 用户信息 |
| `dataset_meta` | 数据集元数据 |
| `algorithm_meta` | 算法元数据与参数模式 |
| `experiment` | 用户保存的实验记录 |
| `training_session` | 训练会话状态 |
| `dataset_data` | 用户上传数据集的行列数据 |
| `experiment_case` | 预设实验案例 |
| `quiz_question` | 练习题题库 |
| `quiz_score` | 用户练习最佳成绩 |

### 6.2 数据库特点

- 算法参数不是写死在前端，而是由数据库中的 `params_schema_json` 动态驱动。
- 实验记录保存用户、算法、数据集、配置 JSON 和最近训练 session。
- 训练 session 保存训练中间状态，便于状态刷新和复盘。
- 实验案例库通过数据库初始化数据提供。

## 七、后端接口说明

### 7.1 认证接口

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/api/auth/login` | 用户登录 |
| POST | `/api/auth/register` | 用户注册 |

### 7.2 元数据接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/algorithms` | 获取算法元数据 |
| GET | `/api/datasets` | 获取数据集元数据 |
| POST | `/api/datasets` | 保存用户上传数据集 |

### 7.3 实验接口

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/api/experiments` | 保存实验 |
| GET | `/api/experiments/history` | 查询用户实验历史 |

### 7.4 实验案例接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/experiment-cases` | 获取预设实验案例 |

### 7.5 训练接口

| 方法 | 路径 | 功能 |
|---|---|---|
| POST | `/api/trainings` | 初始化训练会话 |
| POST | `/api/trainings/{sessionId}/step` | 单步训练 |
| POST | `/api/trainings/{sessionId}/run` | 连续训练 |
| POST | `/api/trainings/{sessionId}/pause` | 暂停训练 |
| POST | `/api/trainings/{sessionId}/stop` | 停止训练 |
| GET | `/api/trainings/{sessionId}` | 获取训练会话摘要 |
| GET | `/api/trainings/{sessionId}/status` | 获取训练状态 |
| GET | `/api/trainings/{sessionId}/stream` | 订阅训练状态流 |

### 7.6 AI 助教接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/ai/status` | 获取 AI 助教状态 |
| POST | `/api/ai/chat/stream` | SSE 流式聊天 |

### 7.7 练习题接口

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/quiz/overview` | 获取各专题题量与个人成绩 |
| GET | `/api/quiz/questions` | 获取某专题题目 |
| POST | `/api/quiz/submit` | 提交作答并判分 |

## 八、前端页面说明

| 页面 | 功能 |
|---|---|
| 首页 | 平台介绍和学习入口 |
| 登录 / 注册页 | 用户登录与注册 |
| 学习仪表盘 | 展示学习进度、推荐任务、最近实验 |
| 课程路径 | 展示机器学习知识地图 |
| 算法教学 | 展示算法理论说明 |
| 算法实验室 | 完成配置、训练、可视化、解释和保存 |
| 结果分析 / 实验记录 | 查看实验历史和模型对比 |
| 数据集广场 | 查看数据集信息 |
| 练习题 | 按大类、专题、试题三级练习，提交后判分并记录最佳成绩 |
| 个人中心 | 展示当前登录状态和用户信息 |

## 九、部署说明

### 9.1 本地运行方式

前置环境：

- Node.js 18+
- npm 9+
- Java 17
- Maven 3.9+
- Python 3.10+

启动 Python 算法服务：

```powershell
cd "D:\高级Web技术pj\python-service"
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

启动后端：

```powershell
cd "D:\高级Web技术pj\backend"
mvn spring-boot:run
```

如果在中文路径下 `mvn spring-boot:run` 遇到主类加载问题，可先打包再运行：

```powershell
cd "D:\高级Web技术pj\backend"
mvn clean package -DskipTests
java -jar target\mlplatform-backend-0.0.1-SNAPSHOT.jar
```

启动前端：

```powershell
cd "D:\高级Web技术pj"
npm start
```

访问：

```text
http://localhost:4200
```

### 9.2 Docker Compose 部署方式

生产环境提供 `docker-compose.prod.yml`，包含：

- `python`：Python FastAPI 算法服务。
- `backend`：Spring Boot 后端服务。
- `frontend`：Nginx 托管 Angular 构建产物。
- `backend-data`：后端 H2 文件数据库挂载卷。

启动命令：

```bash
docker compose -f docker-compose.prod.yml up --build -d
```

线上访问端口：

```text
80
```

如配置 AI 助教，需要设置环境变量：

```bash
APP_AI_API_KEY=你的API_KEY
APP_AI_BASE_URL=大模型服务地址
APP_AI_MODEL=模型名称
```

## 十、使用说明

### 10.1 登录

演示账号：

```text
student / 123456
```

### 10.2 完成一次训练实验

1. 登录系统。
2. 进入算法实验室。
3. 选择学习类型、算法、数据集。
4. 调整参数。
5. 点击“应用配置”。
6. 点击“初始化”。
7. 使用“单步执行”观察细节，或使用“自动训练”观察整体趋势。
8. 查看二维图、Loss 曲线、Accuracy 曲线、评估指标和模型解释。
9. 输入实验名称并保存实验。

### 10.3 查看历史记录

1. 登录后进入“我的实验记录”。
2. 查看保存过的实验。
3. 可回填历史配置进行复现。

### 10.4 使用 AI 助教

1. 点击右下角 AI 助教。
2. 输入与当前实验相关的问题。
3. 查看流式回答。

示例问题：

- 为什么学习率太大会导致 Loss 震荡？
- KMeans 的 K 值应该怎么选？
- 当前模型的 Accuracy 和 F1 应该如何理解？

## 十一、项目验证记录

截至 2026-06-24，已完成如下验证：

| 验证项 | 结果 |
|---|---|
| 前端 `npm run build` | 通过，存在 bundle budget warning，不影响运行 |
| 后端 `mvn test` | 通过，12 tests, 0 failures |
| Python 核心文件 `py_compile` | 通过 |
| 注册接口冒烟测试 | 通过，用户名、邮箱、密码可写入数据库 |
| 练习题接口冒烟测试 | 通过，题库返回、服务端判分与最佳成绩落库正确 |

## 十二、项目特色与创新点

1. 传统机器学习算法覆盖较完整，包含监督学习、无监督学习、强化学习。
2. 支持逐步训练，而不是只展示最终模型结果。
3. 支持动态可视化决策边界、回归线、聚类中心、主成分投影和 GridWorld。
4. 支持模型解释，包括线性权重、树结构、特征重要性、PCA 主成分和 Q-Learning 策略。
5. 支持实验案例库和课程路径，引导初学者按知识点学习。
6. 支持模型对比和特征工程模拟。
7. 集成 AI 助教，支持流式回答和上下文解释。
8. 前后端分离，后端与 Python 算法服务分离，架构清晰，方便扩展。

## 十三、团队分工说明

> 以下内容请根据实际小组成员填写。课程要求需要列出团队成员、分工、具体完成工作和贡献比例。

| 成员 | 学号 | 主要分工 | 具体工作 | 相对贡献比例 |
|---|---|---|---|---|
| 请填写 | 请填写 | 请填写 | 请填写 | 100% |
| 请填写 | 请填写 | 请填写 | 请填写 | 请填写 |
| 请填写 | 请填写 | 请填写 | 请填写 | 请填写 |

## 十四、仍可继续改进的方向

1. 练习题目前为单选题，可扩展为多选、判断或简答题。
2. 对 CSV 数据集提供更完善的数据清洗、缺失值处理和持久化管理。
3. 补充 Web3D、WebRTC 或 WebAssembly 等更强的课程进阶技术亮点。
4. 优化前端 bundle 体积，减少构建预算警告。
5. 增加更多自动化端到端测试。
6. 为 AI 助教增加更明确的当前实验上下文展示。

